using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ARchanges : MonoBehaviour
{
    // Start is called before the first frame update
    // Bar turned off and added a bar to the canvas layer to make it clickable
    //script with camera and transitions need to be fixed

}